//#region lib/index.js
/**
 * Host loader entry for the browser-only RTL chat-box plugin.
 *
 * Provides no host-side behavior: the direction stylesheet and the settings row
 * both live in the browser, and nothing here crosses sessions.
 */
function apply() {}
//#endregion

export { apply }
