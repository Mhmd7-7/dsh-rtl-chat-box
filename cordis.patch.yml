# dsh-rtl-chat-box bundle patch: inserts its public dsh.client entry into the web
# plugin roster. The bundle is the official dsh plugin shape — install it with
# `dsh plugin --profile <name> add <path>`.
- insert:
    - id: ui-rtl-chat-box
      name: 'dsh-rtl-chat-box'
